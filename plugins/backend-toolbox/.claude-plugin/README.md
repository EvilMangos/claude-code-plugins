# Plugin manifest (`.claude-plugin/plugin.json`)

This folder contains metadata used when **packaging/publishing** the `backend-toolbox` plugin.

If you publish this plugin to a registry/marketplace, verify the expected `plugin.json` schema for your target Claude Code runtime and adjust fields accordingly.


